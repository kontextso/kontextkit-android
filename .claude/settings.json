{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path'); case \"$file\" in *.kt|*.kts) which ktlint >/dev/null && ktlint --format \"$file\" 2>/dev/null || true ;; esac"
          }
        ]
      }
    ]
  }
}
